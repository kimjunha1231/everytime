import React from "react";
import styled from "styled-components";

export default const Footer = () => {
    let navigate = useNavigate();
    const [title, setTitle] = useRecoilState(navtitle)
    return (
        <>
        </>
    );
};
